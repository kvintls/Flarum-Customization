# Flarum 论坛部署计划（NodeSeek/Reddit 风格）

## 服务器现状（已排查）
- 系统：Ubuntu 24.04.4 LTS
- 内存：2GB（当前基本吃满，靠 Swap 撑），Swap 2GB
- 已跑：
  - Docker 容器：cloudflared（隧道）、halo（博客/CMS，端口 8090）、halo-mysql（MySQL 8.0，占 ~830MB）
  - xray + x-ui（代理）
  - caddy（管 :80/:443，自动 HTTPS）
- 无 nginx/apache/php（php 需补装，或走 Docker）

## 方案总原则
- 在 Flarum 上二次定制，**不碰内核**
- 颜值/布局 = 自定义 CSS + 主题扩展
- 功能 = 现成扩展
- 放弃"嵌套评论树"，用扁平讨论流（NodeSeek 本身也是扁平流）

## 明天要做的步骤
1. 给 halo-mysql 瘦身（加内存限制参数，省出 300MB+）
2. Docker 部署 Flarum，复用现有 MySQL（新建 flarum 库 + 独立用户）
3. 装扩展：
   - flarum-lang/chinese-simplified（中文包）
   - fof/upvotes（点赞/点踩）
   - Tags（版块）、flarum/mentions（@提醒）、fof/follow-tags（关注版块）
4. Caddy 加反代配置 + Cloudflare Tunnel 加路由（HTTPS）
5. 建测试版块/帖子，开始迭代自定义 CSS 调外观

## 还需用户确定的细节
- [x] 域名：kvin.de5.net（论坛专用新域名）
  - 注意：kcin.eu.cc 已被 /opt/pureget（node 项目，localhost:8787）占用，不动它
  - MySQL root 密码：W4jiZLmE5DdR，网络 halo_default，主机名 halo-mysql / db
  - cloudflared 是 token 模式（路由在 CF 后台面板管理）
  - Caddy 监听 http 80 / https 8443，配置在 /etc/caddy/Caddyfile
- [ ] 列表风格：NodeSeek 紧凑列表 / Reddit 卡片流
- [ ] 投票：点赞+点踩 / 只点赞
- [ ] 配色：浅色 / 深色 / 可切换
- [ ] 版块规划（分几个板块）
- [ ] 是否重点优化移动端

## 方法论提醒
先把可预览环境跑起来，再一边看真实界面一边迭代外观。颜值是"调"出来的。
