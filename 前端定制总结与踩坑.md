# Flarum 前端定制总结与踩坑记录

> 项目: kvin 论坛 (Flarum 1.8.19, Docker 部署, Cloudflare Tunnel 反代)
> 目标: 把默认现代蓝紫风改成 FluxBB / Arch BBS 灰白风,统一前后台样式
> 时间跨度: 几天的高频迭代,期间踩了大量坑,本文档全部归档留底

---

## 目录
1. [部署架构与关键流程](#1-部署架构与关键流程)
2. [Flarum 前端渲染机制核心概念](#2-flarum-前端渲染机制核心概念)
3. [各页面定制记录](#3-各页面定制记录)
4. [通用踩坑与教训](#4-通用踩坑与教训)
5. [关键 CSS 类名参考表](#5-关键-css-类名参考表)
6. [调试方法论与常用命令](#6-调试方法论与常用命令)
7. [Git commit 关键节点](#7-git-commit-关键节点)

---

## 1. 部署架构与关键流程

### 1.1 项目文件与作用

| 文件路径 | 作用 | 部署方式 |
|---------|-----|---------|
| `custom-style-full.css` | 全站 CSS 定制 | **手动**粘到 Flarum Admin > Appearance > Edit Custom Styles |
| `custom-header.html` | 全站 HTML/JS 注入 (导航条 + 各种 setInterval hack) | **手动**粘到 Admin > Appearance > Edit Custom Header |
| `theme/register-page/extend.php` | Flarum 扩展 (整页 /register /login + admin CSS 注入) | 服务器 `/opt/flarum/theme/register-page/` |
| `theme/register-page/less/admin.less` | 后台管理面板 CSS (走扩展注入,不走 Appearance) | 同上 |
| `docker-compose.yml` | 容器配置 (`./theme:/flarum/app/theme` 挂载) | `/opt/flarum/` |

### 1.2 部署流程 (踩坑最多的地方)

**前端 CSS / Header 改动**:
1. GitHub 上 push 完新 commit
2. 拿 **带 commit hash 的 raw URL** (不用 `main` 分支 URL,GitHub CDN 会缓存旧内容):
   ```
   https://raw.githubusercontent.com/kvintls/Flarum-Customization/<COMMIT_HASH>/custom-style-full.css
   ```
3. 打开 raw 页面 Ctrl+A 全选 → 复制
4. Flarum Admin > Appearance > Edit Custom Styles → **全选删掉再粘**(不能在末尾追加)
5. 保存
6. Admin > Advanced > **Clear Cache**
7. 浏览器**硬刷** Ctrl/Cmd+Shift+R (不硬刷会吃浏览器缓存的旧 CSS)

**后台 admin.less 改动** (扩展级):
1. 服务器 `/opt/flarum/` 目录下 `curl` 下载最新 `extend.php` 和 `less/admin.less`
2. `docker exec flarum php flarum cache:clear`
3. `docker restart flarum` (触发 Flarum 重编 admin 资产)
4. 浏览器打开 `/admin` 硬刷

### 1.3 关键限制

- **Flarum Custom LESS 字段有 65000 字符上限** (触发过多次,详见 [4.4 章](#44-65000-字符限制))
- **Custom Styles 只编译进 forum,不进 admin** (详见 [3.5 后台面板](#35-后台管理面板-admin))

---

## 2. Flarum 前端渲染机制核心概念

### 2.1 Mithril SPA

- Flarum 前端用 **Mithril.js** 做单页应用
- 页面切换是 SPA 路由 (`m.route.set()`) —— 不发起完整 HTTP 请求,只重绘 DOM
- 组件通过 `view()` 返回 JSX/hyperscript,Mithril 用 **vdom diff** 决定实际 DOM 改动
- **关键推论**: 我们在 setInterval 里改 DOM 会被 Mithril 下一次 redraw 覆盖回来

### 2.2 forum.less vs admin.less

Flarum 前端资产有两条独立编译流水线:
- `forum.less` → 用户看到的 `/` 论坛前台
- `admin.less` → 管理员看到的 `/admin` 后台

**Appearance > Custom Styles 里的 LESS 只注入到 forum.less**,不会进 admin。要改后台样式必须通过扩展 `Extend\Frontend('admin')->css(__DIR__.'/less/admin.less')` 明确注入。

### 2.3 DOM 结构与 Mithril createElement 的坑

Mithril 生成 DOM 用的是 `document.createElement + appendChild`,**不是** `innerHTML`。这带来一个反直觉的行为:

```jsx
<table class="rankings">
  <tr>...</tr>
  <tr>...</tr>
</table>
```

正常 HTML 解析 (innerHTML) 时,浏览器会自动补 `<tbody>` 包裹所有 `<tr>`。但 Mithril 用 createElement,**浏览器不会补**,最终 DOM 就是:
```html
<table class="rankings">
  <tr>...</tr>
  <tr>...</tr>
</table>
```

任何写 `table.rankings tbody tr {...}` 的选择器都**完全 match 不到** —— 这个坑我踩过很久,详见 [3.1 贡献榜](#31-贡献榜页面-rankings)。

---

## 3. 各页面定制记录

### 3.1 贡献榜页面 /rankings

**扩展**: FriendsOfFlarum/gamification (`fof/gamification`)

**需求演变** (由简到繁):
1. 表头 `排名` / `用户` / `👍` 改成 `贡献榜` / `用户` / `贡献认同`
2. 加载更多按钮换位置 (从底部 → 右上 → 变成第 4 列表头 → 用 rowspan 撑满整表垂直居中)
3. 前 3 名奖杯图标 + 第 4-6 名数字 "4/5/6" 全部替换成随机赞赏词 (论坛之光 / 贡献先锋 …)
4. 表格 4 列平均分 (`table-layout:fixed + width:25%`) → 后来加"最新更新时间"列变 5 列 20%
5. 每行首列加小方块 (模拟复选框视觉)
6. 表格宽度、行高、分隔线、字体颜色多轮调整
7. 未登录用户屏蔽访问

**关键根因**:

#### 根因 A: `<table>` 下无 `<tbody>` (Mithril 特性)

- **现象**: 我写 `table.rankings tbody tr>td:first-child::before{content:"..."}` 全部规则失效,奖杯依旧显示
- **诊断**: `table.rankings th{border-bottom}` 之类不带 tbody 的规则**能**生效,但**带 tbody 的一条都不生效**
- **根因**: `RankingsPage.js` 里 JSX 直接 `<table><tr>...</tr></table>`,Mithril 用 createElement 建 DOM 不触发浏览器 tbody 自动补全
- **修法**: 选择器**去掉 tbody**,改成 `table.rankings tr > td:first-child::before`

#### 根因 B: 表头 `<tr>` 是 `nth-child(1)`,数据行从 (2) 起

- **现象**: 首列赞赏词都往下错了一行,第 1 名用户显示 "贡献先锋"(应该是"论坛之光")
- **根因**: 表头行也是 `<tr>` (里面装 3 个 `<th>`),跟数据行同级 —— `tr:nth-child(1)` 匹配到表头行,数据行第 1 名是 `nth-child(2)`
- **修法**: 所有 nth-child 索引 **+1**

#### 根因 C: `加载更多` 居中失败 —— CSS 特异性抢夺战

- **现象**: 加了 `.kv-loadmore-th{text-align:center!important}` 但按钮仍是左对齐
- **诊断特异性**:
  - `.kv-loadmore-th` = **(0, 1, 0)** (1 个 class)
  - `table.rankings th{text-align:left}` = **(0, 1, 2)** (1 class + 2 element)
- **规则**: CSS 平级 `!important` 时**特异性高者胜**,`table.rankings th` 因为多了 2 个 element 特异性更高,盖过我的 center
- **修法**: 选择器升级为 `table.rankings th.kv-loadmore-th` = **(0, 2, 2)** 胜出

#### 根因 D: 点击后底部按钮闪现

- **现象**: 用 JS `button.style.display='none'` 隐藏原生"加载更多"按钮,点击后 Mithril 重绘生成新按钮 DOM 节点,新节点没 inline 的 `display:none`,在下一轮 setInterval 之前露出来
- **修法**: 用 CSS `.rankings-loadmore{display:none!important}` 直接把整个容器隐藏 —— **CSS 是声明式的,Mithril 每次 render 都受它约束**,不会有闪现

#### 根因 E: 加载更多 rowspan + 垂直居中

- **需求**: 第 4/5 列"加载更多"要贯穿整表垂直居中
- **实现**: JS 每轮 setInterval 计算 `tbl.querySelectorAll('tr').length` 更新 `<th>` 的 `rowspan` 属性 —— th 默认 `vertical-align:middle`,rowspan 撑满自然居中

#### 根因 F: CSS 变量简化 21 条 nth-child 规则

- **原**: 21 条 `.rankings tr:nth-child(N) td:first-child::before{content:"XXX"}` 每条约 60 字符 = 1260 字符
- **优化**:
  ```css
  .rankings tr td:first-child::before{content:var(--kv-p,"贡献者");...}
  .rankings tr:nth-child(2){--kv-p:"论坛之光"}
  .rankings tr:nth-child(3){--kv-p:"贡献先锋"}
  ...
  ```
  CSS 自定义属性会**从 tr 继承到内部 ::before**,`content:var()` 读取。省 ~500 字符。

#### 根因 G: 切页时闪现"排名 | 用户 | 👍"旧界面

- **现象**: 从首页点导航"贡献榜",Mithril 一渲染完就露出原生 `排名/用户/👍`,setInterval 400ms 后才改成 `贡献榜/用户/贡献认同`
- **修法**:
  - CSS: `table.rankings:not(.kv-ready){visibility:hidden!important}` (占位不变,不会布局跳)
  - JS: 每轮 setInterval 完成所有改动后最后一步 `tbl.classList.add('kv-ready')`
  - setInterval 频率从 400ms → 50ms
- **净效果**: 表格默认不可见,50ms 内 JS 完成所有 DOM 改动 + 加 ready 类,瞬间以最终形态显现

#### 未登录屏蔽访问

```js
if(!(window.app && app.session && app.session.user) && location.pathname === '/rankings'){
  app.route.set('/');  // SPA 跳转
}
```
同时在 `<a href="/rankings">` 上加 `display:none` 隐藏导航链接。

---

### 3.2 用户页 /u/{username}

**组件**: `UserPage.tsx` + `UserCard.tsx` + `AffixedSidebar.tsx`

#### 根因 A (最严重): `.UserCard--hero` 类根本不存在

- **表象**: 我写的所有针对绿色横幅的规则(去背景色/改灰/隐藏等)**通通没生效**,用户反复反馈"没变化"
- **诊断**: 让用户在 Console 跑
  ```js
  getComputedStyle(document.querySelector('.UserCard--hero')).display
  ```
  报 `TypeError: parameter 1 is not of type 'Element'` → 说明 `.UserCard--hero` 的 querySelector 返回 `null` = **该 class 不存在**
- **根因**: 我一直按 BEM 双短线写选择器,但 Flarum 源码里:
  ```jsx
  <UserCard className="Hero UserHero" ... />
  ```
  UserCard 组件根 class 为 `classList('UserCard', this.attrs.className)`,输出的真实 DOM 是:
  ```html
  <div class="UserCard Hero UserHero">
  ```
  **三个独立 class,不是 `UserCard--hero`**
- **修法**: 全部改用 `.Hero.UserHero{display:none!important}` (匹配同时有 Hero 和 UserHero 两个 class 的元素,精确命中用户页横幅,不误伤帖子里悬停 UserCard)

**教训**: **修 CSS 之前必须去 GitHub 拉 Flarum 组件源码看 JSX 真实输出的 className**,不能凭猜。

#### 根因 B: sideNav 巨大空白 —— Flarum 默认的 position:absolute 布局

- **现象**: 左侧栏和右侧内容之间有 200px 空白
- **根因**: Flarum 默认给 `.sideNav` 加 `position:absolute; left:0`,给 `.sideNavOffset` 加 `margin-left:~220px` 模拟侧边栏布局。我改成 flex 时没覆盖这两条,导致定位方式叠加
- **修法**: 显式设 `.UserPage .sideNav{position:static!important} .UserPage .sideNavOffset{margin-left:0!important}` + 用 flex 布局

#### 根因 C: 侧边栏滚动变窄 —— Bootstrap Affix 插件

- **现象**: 页面往下滚,左侧 tabs 突然变窄并浮到顶部
- **根因**: Flarum 的 `AffixedSidebar` 组件调用 Bootstrap `.affix()` 插件,**给 sideNav 内部的 `<ul>`** 加 `.affix` / `.affix-top` / `.affix-bottom` 类 + inline style (`position:fixed; width:X`),脱离布局
- **修法**: 强制覆盖 ul 的 affix 状态
  ```css
  .UserPage .sideNav ul,
  .UserPage .sideNav ul.affix,
  .UserPage .sideNav ul.affix-top,
  .UserPage .sideNav ul.affix-bottom{
    position:static!important;
    width:auto!important;
    top:auto!important;
  }
  ```

#### "套两层"的层级设计

用户要求外框套一层视觉,最终布局:
```
┌───────────────────────────────────────────┐
│ .UserPage>.container (外层大白卡, 16px padding)│
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │ .sideNavContainer (内层卡, flex 布局)  │ │
│  │  ┌────────┐│┌────────────────────┐   │ │
│  │  │左 tabs ││├─│  右内容             │   │ │
│  │  └────────┘│└────────────────────┘   │ │
│  └───────────────────────────────────────┘ │
│  ┌───────────────────────────────────────┐ │
│  │ #kvin-footer2 (蓝色顶线 + 页脚文字)     │ │
│  └───────────────────────────────────────┘ │
└───────────────────────────────────────────┘
```

选择器用 `.UserPage>.container` (**直接子选择器** `>`) 避免误伤 Hero 内部隐藏的 `.container`。

`#kvin-footer2` 通过 `custom-header.html` 里的 setInterval JS 挪进 `.UserPage>.container`,与 `.sideNavContainer` 同级。

---

### 3.3 设置/通知页 /u/{username}/settings

**组件**: `SettingsPage.tsx` extends `UserPage`

#### 根因 A: 用错选择器 `.Form-group`

- **现象**: 我给 `.UserPage-content:has(table) .Form-group` 加了边框,但页面上根本看不到框
- **根因**: 设置页里根本**没有 `.Form-group` 这个 class**
- **实际结构** (拉 Flarum 源码看 SettingsPage.tsx 得知):
  ```jsx
  <div className="SettingsPage">
    <ul>
      <li className="item-account">
        <FieldSet className="Settings-account" label="账户设置">
          {...}
        </FieldSet>
      </li>
      <li className="item-notifications">
        <FieldSet className="Settings-notifications" label="通知中心">
          <NotificationGrid />
        </FieldSet>
      </li>
      <li className="item-privacy">...</li>
    </ul>
  </div>
  ```
  FieldSet 组件渲染成 `<fieldset class="Settings-XXX"><legend>标题</legend><ul>...</ul></fieldset>`
- **修法**: 选择器改成 `.SettingsPage fieldset` + `.SettingsPage fieldset>legend`

#### 根因 B: 2 列 grid 布局导致大空白

- **尝试**: 用 `grid-template-columns: 1fr 1fr` 让设置项 2 列
- **问题**: `帐户设置` 只有 2 个按钮,占 50% 宽度但内容只 200px → 右边一大片空白
- **修法**: 放弃 grid,改成 `display:block` 单列堆叠 —— 各设置块从上到下,每块独立带边框,视觉干净无空白

#### 通知矩阵 table 宽度

- **需求**: 通知中心的 `<table>` 铺满外框
- **默认**: `<table>` 是 `width:auto`,跟着内容,只占 ~380px 左边一半
- **修法**: `.SettingsPage .NotificationGrid table{width:100%!important}` 强制 100%

---

### 3.4 讨论列表 DiscussionListItem

#### 根因: 50+ 条重复 `.DiscussionListItem` 规则堆叠

- **现象**: 用户反复投诉"没有分隔线"、"hover 蓝色阴影"。我加规则,用户说"没变化"
- **诊断** (最后才做的): 全文 grep `.DiscussionListItem`,发现 CSS 里有 **50+ 条相关规则**,包括多条:
  - `.DiscussionListItem{border-bottom:1px solid #eef1f4}` (line 119)
  - `.DiscussionListItem{border-bottom:1px solid #e4e8ec}` (line 219)
  - `.DiscussionListItem:hover{background:#f4f8fc}` (line 20)
  - `.DiscussionListItem:hover{background:#f6f8fa}` (line 120)
  - `.IndexPage .DiscussionListItem{border-bottom:1px solid #e9edf0}` (line 234)
  - …
- **CSS 后来者胜规则**: 同特异性 + `!important` 时,**源码位置靠后**的赢。line 219 的 `#e4e8ec`(几乎透明)盖过 line 18 的 `#d5d9df`(深灰)
- **进一步**: 我把新规则加到文件**末尾**期望胜出,但用了 `.DiscussionListItem` (特异性 0,1,1)。而 line 234 的 `.IndexPage .DiscussionListItem` (特异性 **0,2,1**) 特异性更高。**特异性差异优先于源码位置**,line 234 依旧赢
- **最终修法**: 末尾规则加 `.App` 前缀 → `.App .DiscussionListItem` (0,2,1) **与 line 234 平级**,靠**源码位置更靠后胜出**

**教训**: 大文件里同类规则堆叠是灾难。修改前要:
1. `grep -n '<class-name>'` 看有多少条同选择器
2. 计算目标规则的特异性
3. 保证新规则特异性 ≥ 最高特异性的对手,或**加更具体的祖先选择器**

---

### 3.5 后台管理面板 /admin

**最大踩坑**: Flarum 的 Appearance > Custom Styles **只编译进 forum,不进 admin**。

- **现象**: 我在 Custom Styles 里加 `.App--admin`、`.AdminNav` 之类规则,后台无任何变化
- **根因**: Flarum 有两条独立的资产编译流水线:
  - `forum.less` (自动包含 Custom Styles)
  - `admin.less` (**不包含** Custom Styles)
- **修法**: 通过扩展 `Extend\Frontend('admin')->css(__DIR__.'/less/admin.less')` 显式注入

具体做法(直接挂到已有的 `register-page` 扩展上,不用新装扩展):

`theme/register-page/extend.php`:
```php
return [
    (new Extend\Routes('forum'))
        ->get('/register', 'kvin.register', RegisterController::class)
        ->get('/login', 'kvin.login', LoginController::class),

    (new Extend\Frontend('admin'))
        ->css(__DIR__ . '/less/admin.less'),
];
```

`theme/register-page/less/admin.less` 里写 FluxBB 化的 admin 样式规则 (`.App--admin`, `.AdminHero`, `.AdminNav`, `.Form-group`, `.ExtensionListItem` 等)。

**部署步骤特殊**: 不是粘 CSS 到后台,而是:
1. 服务器 `curl` 更新 `extend.php` 和 `admin.less`
2. `docker exec flarum php flarum cache:clear`
3. `docker restart flarum` (触发 Flarum 重编 admin 资产)
4. 浏览器硬刷 `/admin`

---

## 4. 通用踩坑与教训

### 4.1 CSS 特异性

**特异性表示法**: `(a, b, c)` 三元组,从左往右比较:
- **a**: ID 数量 (`#foo`)
- **b**: class / 属性选择器 / 伪类数量 (`.foo`, `[attr]`, `:hover`, `:nth-child`)
- **c**: 元素 / 伪元素数量 (`div`, `::before`)

关键点:
- `!important` 只把优先级提升到"important 层",**同层内还是要比特异性**
- 平级 + 同 `!important` → **源码位置靠后者胜**
- `.a.b` (0,2,0) 平级 `.foo` (0,1,0),后者不加 !important 前者不加也一样,前者赢

**贡献榜第 4 列 `加载更多` 居中的完整推理**:
- 全局规则: `table.rankings th,table.rankings td{text-align:left!important}` → 特异性 (0, 1, 2)
- 我加: `.kv-loadmore-th{text-align:center!important}` → 特异性 (0, 1, 0)
- 比较: 0=0, 1=1, 但 element 数 0 < 2 → 全局规则胜,加载更多依旧左对齐
- 修: `table.rankings th.kv-loadmore-th{text-align:center!important}` → 特异性 (0, 2, 2)
- 比较: 0=0, class 数 2 > 1 → **胜**

`!important` 平级时提升特异性最简单的方式: **加祖先 class 前缀** (`.App .DiscussionListItem` / `body .foo` / `#app .bar`)。

### 4.2 Mithril vs 手动 DOM 操作

- Mithril 用 `document.createElement + appendChild` 建 DOM
- **反例**: 不触发浏览器 `<table>` 下 tbody 自动补全 → 选择器不能用 `tbody`
- **反例**: 手动 `element.textContent = "X"` 会被 Mithril 下次 redraw 覆盖回 vdom 值
- **反例**: 手动 `parent.appendChild(customNode)` 可能被 Mithril 移走或引起 vdom diff 错乱
- **最佳实践**:
  - 能用 CSS 就用 CSS (`::before`, `::after`, `content:""` 都不受 Mithril 影响)
  - 必须 JS 改 DOM 时用 `setInterval(fn, 短间隔)` **反复应用**,而不是加"是否处理过"的 dataset 门闩
  - Mithril 每次 render 会重置 DOM 到 vdom,setInterval 下一轮再改回来 —— 拼手速

### 4.3 CSS 变量继承 (`--kv-p` 技巧)

- CSS custom property 会**沿着 DOM 树往下继承**,伪元素 (`::before/::after`) 也继承
- **应用**: 21 条 nth-child 分别设 content 简化成:
  ```css
  .rankings tr td:first-child::before{content:var(--kv-p,"贡献者");...}
  .rankings tr:nth-child(2){--kv-p:"论坛之光"}     /* 在 tr 上声明 */
  .rankings tr:nth-child(3){--kv-p:"贡献先锋"}
  ...
  ```
  tr 上的 `--kv-p` 继承到内部 td 的 ::before,`content:var(--kv-p)` 读取。每行只需一个短选择器,省 ~500 字符。

### 4.4 65000 字符限制

Flarum Custom LESS 字段有 65000 字符上限,超了会报 `custom less 须在 65000 个字符以内`,直接**拒绝保存整个 CSS**(不是截断,是全部丢失,导致所有前端定制失效)。

**压缩策略** (逻辑零改动):
1. 去除所有多行 `{...}` 内的换行,合并到单行
2. 去掉 `:` `,` `;` 周围空格
3. 去掉规则末尾 `}` 前的 `;`
4. 去掉重复的 `.App-composer.kv-inline{...}` 等历史堆叠规则 (确认逻辑等价)
5. 长选择器用 CSS 变量替换 (见 4.3)
6. 合并共享属性的选择器为逗号列表

**踩坑: 压缩脚本对嵌套字符串处理错误**:
- 原始规则: `background:url('data:image/svg+xml;...<svg xmlns="http://..." width="14" .../>')`
- 压缩脚本先 stash 所有双引号字符串 → SVG 内部属性值被替换成 `\x00N\x00` 占位符
- 再 stash 单引号字符串 → 外层单引号里带占位符的整个 URL 被 stash
- restore 时**只做了一层 pass**,只把最外层的 strings[N] 换回来,内部的占位符没 restore
- 最终写到文件里就是**裸的 null 字节 + 数字**,浏览器/LESS 解析报 `Expected ')'`
- **修法**: 手工把 SVG URL 规则改回来。以后压缩要么跳过 `url()`,要么循环 restore 直到无占位符

### 4.5 GitHub Raw CDN 缓存

- `https://raw.githubusercontent.com/{user}/{repo}/main/{file}` 会被 GitHub CDN 按 URL 缓存 —— 同一 URL 拉多次可能拿到旧内容
- **解决**: 用**具体 commit hash 的 URL** (每次 push 后拿最新 hash):
  ```
  https://raw.githubusercontent.com/kvintls/Flarum-Customization/<HASH>/custom-style-full.css
  ```
- 每个 commit hash 对应一个独立缓存 key,保证是最新的
- 命令: `git rev-parse HEAD` 拿最新 commit hash

### 4.6 部署流程细节

- Custom Styles 输入框粘贴时**必须全选删完再粘新的**,不能在末尾追加(否则会拼接出问题,或者字符超限)
- 保存 → Clear Cache → 浏览器硬刷,**三步缺一不可**
- 浏览器不硬刷 (Ctrl+Shift+R),会吃 CSS 文件的浏览器缓存,看到的还是旧样式,以为"没生效"
- 服务器 Cloudflare Tunnel 可能也有边缘缓存,但 Flarum 生成的 CSS 文件带 hash suffix,通常自动 bust

### 4.7 调试方法论 (血泪总结)

**修任何选择器之前**必须做这几件事,否则 100% 会花大量时间瞎猜:

1. **拉真实的 Flarum 源码看 DOM 结构** — 用 gh CLI:
   ```bash
   gh api repos/flarum/framework/contents/framework/core/js/src/forum/components/UserPage.tsx?ref=v1.8.9 -H "Accept: application/vnd.github.raw"
   gh api repos/FriendsOfFlarum/gamification/contents/js/src/forum/components/RankingsPage.js?ref=1.x -H "Accept: application/vnd.github.raw"
   ```
   看 JSX 里 `className=` 后跟的字符串**就是真实 DOM class**,别脑补 BEM。

2. **让用户在浏览器 Console 跑 querySelector 验证**:
   ```js
   document.querySelector('.my-selector')  // 返回元素说明存在, null 说明不存在
   getComputedStyle(document.querySelector('.foo')).display  // 看规则最终 computed value
   ```
   —— 用户 `getComputedStyle('.UserCard--hero')` 抛 TypeError 就是决定性证据。

3. **全文 grep 同选择器**看有没有堆叠规则:
   ```bash
   grep -n '.DiscussionListItem' custom-style-full.css | wc -l
   ```
   数量超过 5 就要小心特异性/后来者胜的问题。

4. **算特异性** 再决定选择器写法(见 4.1)。

5. **不要凭表象猜根因**,要看 devtools 的 Elements 面板 → 右下角 Styles → 看**具体是哪条规则生效**、哪条被划掉、为什么被划掉。让用户截 devtools 图。

---

## 5. 关键 CSS 类名参考表

### 5.1 用户页 UserPage

| 用途 | 真实 class | 备注 |
|-----|-----------|-----|
| 页面根 | `.UserPage` | body 上还会有 `.App--user` |
| 顶部横幅 (绿色) | `.UserCard.Hero.UserHero` | **三个独立 class**,不是 `.UserCard--hero` |
| 头像 | `.UserCard--hero .Avatar` (❌错) → `.Hero.UserHero .Avatar` | 同上 |
| 外层容器 (要套框用) | `.UserPage>.container` | `>` 避免误伤 Hero 内的 `.container` |
| 侧边栏容器 | `.UserPage .sideNavContainer` | flex 布局的父 |
| 左侧 tab 栏 | `.UserPage .UserPage-nav.sideNav` | `<nav class="sideNav UserPage-nav">` |
| Affix 内部 ul | `.UserPage .sideNav ul.affix` 等 | Bootstrap 加的类 |
| 右侧内容区 | `.UserPage .UserPage-content.sideNavOffset` | `<div class="sideNavOffset UserPage-content">` |

### 5.2 设置页 SettingsPage

| 用途 | 真实 class |
|-----|-----------|
| 页面根 (在 UserPage-content 内) | `.SettingsPage` |
| 顶层 ul | `.SettingsPage > ul` |
| 每个设置块 li | `.SettingsPage > ul > li.item-account` / `li.item-notifications` / `li.item-privacy` |
| 设置块容器 | `.SettingsPage fieldset.Settings-account` 等 |
| 设置块标题 | `.SettingsPage fieldset > legend` |
| 通知矩阵 | `.NotificationGrid` (内含 `<table>`) |

### 5.3 讨论列表

| 用途 | 真实 class | 备注 |
|-----|-----------|-----|
| 列表容器 | `.DiscussionList-discussions` | ul |
| 每一项 | `.DiscussionListItem` | 特异性 (0,1,1) |
| 页面特定项 | `.IndexPage .DiscussionListItem` | 特异性 (0,2,1) 更高 |
| 项内 title | `.DiscussionListItem-title` |
| 项内内容包装 | `.DiscussionListItem-content` |

### 5.4 贡献榜 rankings (fof/gamification)

| 用途 | 真实 class / 结构 |
|-----|----------------|
| 表格 | `table.rankings` (无 `<thead>` / `<tbody>`!) |
| 表头行 | `.rankings > tr:first-child` = `tr:nth-child(1)` (装 `<th>`) |
| 数据行 | `.rankings > tr` (装 `<td>`) 从 `nth-child(2)` 起 |
| 数据行 rank class | `<tr class="ranking-1">` / `ranking-2` / `ranking-3` |
| 首列 td (前 3 名) | `<td class="rankings-mobile rankings-N">` 里装 `<i class="fas fa-trophy">` |
| 首列 td (4+名) | `<td class="rankings-4 rankings-mobile">数字</td>` |
| 用户列 | `<td><div class="PostUser"><h3 class="PostUser-name rankings-info"><a><span class="username">名字</span></a></h3></div></td>` |
| 加载更多容器 | `.rankings-loadmore` (`<table>` 后面的 `<div>`) |

### 5.5 后台管理面板 admin

| 用途 | 真实 class |
|-----|-----------|
| body 根 | `.App--admin` |
| 顶部条 | `.App-header` |
| 侧边栏 | `.AdminNav` |
| Hero 标题栏 | `.AdminHero` (+ `h2` 是 `.AdminHero-title`) |
| 表单模块 | `.Form-group` (在 admin 里存在) |
| 扩展项 | `.ExtensionListItem` / `.ExtensionCard` |
| 主要按钮 | `.Button--primary` |

---

## 6. 调试方法论与常用命令

### 6.1 拉 Flarum 官方组件源码

```bash
# 用 gh CLI (环境里预配置好)
gh api repos/flarum/framework/contents/framework/core/js/src/forum/components/UserPage.tsx?ref=v1.8.9 -H "Accept: application/vnd.github.raw"
gh api repos/flarum/framework/contents/framework/core/js/src/common/components/FieldSet.js?ref=v1.8.9 -H "Accept: application/vnd.github.raw"
gh api repos/flarum/framework/contents/framework/core/js/src/forum/components/UserCard.js?ref=v1.8.9 -H "Accept: application/vnd.github.raw"
```

### 6.2 拉扩展源码

```bash
# fof/gamification
gh api repos/FriendsOfFlarum/gamification/contents/js/src/forum/components/RankingsPage.js?ref=1.x -H "Accept: application/vnd.github.raw"
gh api repos/FriendsOfFlarum/gamification/contents/js/src/forum/components/RankingImage.tsx?ref=1.x -H "Accept: application/vnd.github.raw"

# 列扩展所有组件
gh api "repos/FriendsOfFlarum/gamification/contents/js/src/forum/components?ref=1.x" --jq '.[].name'
```

### 6.3 浏览器 Console 常用验证

```js
// 检查选择器是否 match
document.querySelectorAll('.my-selector').length

// 检查规则最终值
getComputedStyle(document.querySelector('.foo')).display
getComputedStyle(document.querySelector('.bar')).getPropertyValue('border-bottom')

// 检查 CSS 变量继承值
getComputedStyle(document.querySelector('tr:nth-child(2)')).getPropertyValue('--kv-p')

// 看元素上应用的所有规则(需 devtools Elements 面板)
// F12 → Elements → 选中元素 → Styles 面板会显示所有 matched rules 及被划掉的规则
```

### 6.4 本地 CSS 分析

```bash
# 统计某选择器出现次数(检查堆叠)
grep -c '.DiscussionListItem' custom-style-full.css

# 找所有 :hover 规则
grep -n ':hover' custom-style-full.css

# 找所有含指定 class 的规则行
grep -n 'AdminNav\|.App--admin' custom-style-full.css

# 字符数统计(Flarum 65000 上限)
python3 -c "
with open('custom-style-full.css') as f: c=f.read()
print('chars:', len(c), 'bytes:', len(c.encode('utf-8')))
"
```

### 6.5 GitHub 部署

```bash
# push 完拿最新 commit hash 给用户
git rev-parse HEAD

# 用带 hash 的 raw URL 绕过 CDN 缓存
echo "https://raw.githubusercontent.com/kvintls/Flarum-Customization/$(git rev-parse HEAD)/custom-style-full.css"
```

### 6.6 服务器上部署扩展代码

```bash
cd /opt/flarum

# 下载最新扩展文件
curl -fsSL -o theme/register-page/extend.php \
  https://raw.githubusercontent.com/kvintls/Flarum-Customization/<HASH>/theme/register-page/extend.php
curl -fsSL -o theme/register-page/less/admin.less \
  https://raw.githubusercontent.com/kvintls/Flarum-Customization/<HASH>/theme/register-page/less/admin.less

# 触发重编+清缓存
docker exec flarum php flarum cache:clear
docker restart flarum
```

---

## 7. Git commit 关键节点

按时间倒序,标注关键根因修复:

| Commit (前 7 位) | 主题 | 重要性 |
|---------|------|-------|
| `aa466b7` | 后台样式改通过扩展注入 (根因: Custom Styles 不进 admin) | ⭐⭐⭐ |
| `5b666e0` | 讨论列表根因彻底修 (.App 前缀提特异性) | ⭐⭐⭐ |
| `4725b71` | 用户页 CSS 完全没生效根因 (UserCard--hero 类根本不存在) | ⭐⭐⭐⭐⭐ |
| `13ac7fa` | 修复 LESS 解析错误 (压缩脚本 SVG null byte bug) | ⭐⭐⭐ |
| `8388f6d` | CSS 变量替换 21 条 nth-child (省 500 字符) | ⭐⭐ |
| `5c02c46` | 贡献榜改成 4 列平均分 (rowspan + 加载更多) | ⭐⭐⭐ |
| `65f4e78` | 贡献榜根因: `<table>` 下无 `<tbody>` | ⭐⭐⭐⭐⭐ |
| `316379d` | 首列奖杯/数字用 CSS ::before 覆盖 (避 Mithril 重绘) | ⭐⭐ |
| `3fa05ff` | 关闭编辑框跨页漂 (kvPrevPath 通用条件) | ⭐⭐ |
| `1c20daf` | visibility:hidden 直到 JS 加 kv-ready 类 (消除切页闪现) | ⭐⭐ |
| `940e7a7` | 加载更多居中根因 (特异性升到 (0,2,2)) | ⭐⭐⭐ |
| `ada486b` | 贡献榜未登录屏蔽 (导航链接隐藏 + 直接访问跳转) | ⭐ |

---

## 附录: 一些奇葩现象与解释

- **粘了 CSS 但一直"没变化"** → 99% 是 LESS 有解析报错被 Flarum 拒绝保存(那个红色 toast "Expected ')' in anonymous-file..."),整个 Custom Styles 字段被丢弃。修完报错重试。
- **偶发"点击后底下冒出一个按钮"** → Mithril redraw 的一瞬,新 DOM 节点没沿用 JS inline style。改用 CSS 声明式规则覆盖,不用 JS 改样式。
- **F5 刷新和 Ctrl+Shift+R 硬刷区别** → F5 用磁盘缓存,Ctrl+Shift+R 强制重新下载。CSS 类改动必须硬刷。
- **改完后台一切正常但 forum 崩了 / 反之** → 忘了 `docker exec flarum php flarum cache:clear`,资产没重新编译。
- **反反复复出现旧样式** → 一定是 GitHub raw CDN 缓存,永远用 commit hash URL。



---

# 第二阶段: 后端集成 + 交互功能踩坑集 (fof/gamification + 表情包 + 投票 + 用户主页统计)

> 本节记录 2025 年 9 月这次会话遇到的所有问题, 按"从容易踩到低到高"排序, 每条都写清楚**现象 / 诊断 / 根因 / 修法 / 教训**, 以便下次能一眼避开。

## 8. Flarum CLI 命令静默退出 (WORKDIR 陷阱)

### 8.1 现象
`docker exec flarum php flarum extension:enable fof-gamification`, `migrate`, `cache:clear` 全部**返回空**, 什么都不打印, 但也不报错。以为扩展启用成功了, 结果 SQL 查 `flarum_users` 发现 `points` / `votes` 列都没建。

### 8.2 诊断
换成 `docker exec flarum sh -c "cd /flarum/app && php flarum extension:list"`, CLI 立刻能正常输出 (虽然还是报"命令不存在", 但至少能报错) —— 说明工作目录是关键。

### 8.3 根因
`docker exec flarum php flarum <cmd>` 使用**容器默认 WORKDIR** 执行, 而这个镜像的默认 WORKDIR 不是 `/flarum/app`。Flarum CLI 需要在 app 目录里跑才能找到 `config.php` 完成 boot, 找不到 config 就**直接静默 exit**, 不打印任何提示。

### 8.4 修法
所有 flarum CLI 命令加 `cd /flarum/app && ` 前缀:
```bash
docker exec flarum sh -c "cd /flarum/app && php flarum extension:enable fof-gamification"
docker exec flarum sh -c "cd /flarum/app && php flarum migrate"
docker exec flarum sh -c "cd /flarum/app && php flarum cache:clear"
```

### 8.5 教训
- 任何 Flarum CLI 出问题, **第一件事先加 `cd /flarum/app`** 重跑一次
- Flarum boot 失败时是 silent exit (没有 stderr 输出), 特别隐蔽; 加 `; echo EXIT=$?` 能看到退出码判断成功与否
- 也可以走 `docker exec -w /flarum/app flarum php flarum <cmd>` 效果一样

---

## 9. `fof/upvotes` composer 包不存在 (幽灵包)

### 9.1 现象
`docker exec flarum sh -c "cd /flarum/app && composer require fof/upvotes"` 报错:
```
Could not find a matching version of package fof/upvotes.
Check the package spelling, your version constraint and that the package
is available in a stability which matches your minimum-stability (stable).
```

### 9.2 根因
**fof/upvotes 这个包在 Packagist 上根本不存在**。Flarum 生态里的"上下投票"功能**内建在 fof/gamification 里**了, 不是独立扩展。

### 9.3 修法
直接用 fof/gamification 自带的投票 UI。gamification 装了之后, 帖子头部会出现 `.Post-vote.Post-upvote` / `.Post-vote.Post-downvote` 按钮 (DOM 结构: `LI.item-votes > DIV.CommentPost-votes > BUTTON.Post-vote.Post-upvote`)。

### 9.4 教训
- 装扩展前先在 https://extiverse.com 或 Packagist 搜确认存在再跑 composer
- FoF 扩展命名不总是"功能名":
  - **fof/gamification** = 上下投票 + 分数聚合 + 排行榜 (三合一)
  - **fof/upvotes** = 不存在
  - **flarum/likes** = Flarum 官方的简单"赞", 独立于 gamification, 走 `flarum_post_likes` 表

---

## 10. `extension:list` 命令不存在

### 10.1 现象
`php flarum extension:list` 报错:
```
Command "extension:list" is not defined.
Did you mean one of these?
  extension:disable
  extension:enable
  extensions:sync-abandoned
  schedule:list
```

### 10.2 根因
Flarum CLI **没有** `extension:list` 命令。只有 enable / disable 单点操作, 没有列出全部扩展的命令。

### 10.3 修法
两种正确方式:

**A) 用 `flarum info`** (dump 完整环境信息, 包括扩展列表)
```bash
docker exec flarum sh -c "cd /flarum/app && php flarum info"
```

**B) 直接查数据库** (最快, 一行输出所有启用扩展)
```bash
docker exec halo-mysql mysql -uflarum -p'XXX' flarum \
  -e "SELECT value FROM flarum_settings WHERE \`key\`='extensions_enabled';" 2>/dev/null
```
返回一段 JSON, 里面是所有启用的扩展 ID (如 `"fof-gamification"`, `"flarum-likes"` 等)。

### 10.4 教训
- Flarum CLI 命令总览: `docker exec flarum sh -c "cd /flarum/app && php flarum list"` 会列出所有可用命令
- 想看扩展状态优先查 `flarum_settings` 表, 一劳永逸

---

## 11. flarum-likes ↔ fof/gamification 不自动打通

### 11.1 现象
用户点了帖子头部的"赞"按钮 (显示"取消赞"表明 like 生效了), 但 `SELECT votes FROM flarum_users` 里全是 0, 前端侧栏"贡献值"也不更新。

### 11.2 诊断
```sql
SELECT * FROM flarum_post_likes LIMIT 5;    -- 有数据 (flarum-likes 写的)
SELECT * FROM flarum_post_votes LIMIT 5;    -- 也有数据 (但是老数据, 不是刚点赞的)
```
两张表都有内容, 但用户 `votes` 字段没变化。

### 11.3 根因
**flarum-likes 和 fof/gamification 是两套独立系统, 不互通**:

| 系统 | 触发方式 | 写入表 | gamification 是否监听 |
|---|---|---|---|
| flarum-likes | 头部灰条"赞/取消赞"文字按钮 | `flarum_post_likes` (post_id, user_id) | ❌ 不监听 |
| fof/gamification 投票 | `.CommentPost-votes` 里的 ▲▼ 图标按钮 | `flarum_post_votes` (post_id, user_id, value=+1/-1) | ✅ 监听自己的表, 自动聚合到 `flarum_users.votes` |

用户视觉上分不清哪个是哪个, 因为**两组按钮都塞在 Post-actions 灰条里**, 一堆文字混排。

### 11.4 修法
两条路:

**A) 只保留 gamification, 禁用 flarum-likes** (推荐, UX 干净)
```bash
docker exec flarum sh -c "cd /flarum/app && php flarum extension:disable flarum-likes"
```

**B) 保留两套, 视觉上把 gamification 按钮设计得跟 flarum-likes 明显不同**
本次采用: 把 gamification 按钮的 `.Button-icon` 隐藏, 用 `::before content:"贡献加一/贡献减一"` 填文字, 让用户一眼分辨语义。

### 11.5 教训
- **一装两个投票插件必检查是否互通** —— 大部分插件只监听自己的表, 事件不会互相传播
- **fof/gamification 的"官方伴侣"是 fof/upvotes** (它俩共用 `flarum_post_votes` 表), 但 fof/upvotes 不存在于 Packagist (见第 9 节), 所以只能用 gamification 自带 UI 或者禁用 flarum-likes 二选一
- 装完后一定要**真机点一次 → 立刻查 DB** 验证链路, 不能光看前端反馈

---

## 12. fof/gamification 字段名不是 `points`, 是 `votes`

### 12.1 现象
前端 `computeXP` 里我预写了 `user.data.attributes.points` 读取真实分数, 装完插件发现 `flarum_users` 里根本没有 `points` 列, `SHOW COLUMNS FROM flarum_users LIKE 'points'` 空返回。

### 12.2 诊断
`SHOW COLUMNS FROM flarum_users;` 完整列表, 找到新增字段:
```
votes           int          NOT NULL   -- fof/gamification 加的分数字段
rank            varchar(255) YES        -- 存等级"名字符串"(不是数字)
last_vote_time  datetime     YES        -- 最后一次投票时间
```

附带建了 3 张表:
- `flarum_post_votes` (投票明细: post_id, user_id, value)
- `flarum_ranks` (等级定义: points 阈值 → 组 id 映射)
- `flarum_rank_users` (用户到等级的关联)

### 12.3 根因
fof/gamification 用 `votes` 而不是 `points`, 我最初凭直觉猜 `points` 错了。

### 12.4 修法
前端 `computeXP` 改成先试 `votes`, 再试 `points` / `reputation` 兜底:
```js
function computeXP(user, days){
  try{
    var attrs = (user.data && user.data.attributes) || {};
    var candidates = ['votes', 'points', 'reputation'];
    for(var i = 0; i < candidates.length; i++){
      var v = attrs[candidates[i]];
      if(typeof v === 'number' && !isNaN(v)) return Math.max(0, Math.floor(v));
    }
  }catch(e){}
  // 兜底: heuristic
  ...
}
```

### 12.5 教训
- **装扩展前先看它的 migration 源码** (`vendor/fof/gamification/migrations/*.php`), 一目了然新加了哪些列
- 或者装完后立刻 `SHOW COLUMNS FROM flarum_users` 看新字段
- 前端读远程字段用 `typeof v === 'number'` 判断, **不能用 `attrs.votes || attrs.points` 因为 0 会被 `||` 当 falsy 漏掉真实 0 分**

---

## 13. `flarum_post_votes` 有数据但 `flarum_users.votes` 全是 0 (老数据未聚合)

### 13.1 现象
`flarum_post_votes` 表里有 3 条投票记录 (post 21/22/23 都有 admin 投的 value=+1 或 -1), 时间戳是 gamification 启用之前的老数据。但 `flarum_users.votes` 全是 0, 完全没聚合到用户身上。

### 13.2 根因
fof/gamification 只在**监听器触发时**才增量更新 `flarum_users.votes`。老投票数据插入到 `flarum_post_votes` 时, gamification 还没启用, 事件没触发, 所以聚合值一直是 0。

### 13.3 修法
**推荐**: 用 fof/gamification 自带的 CLI 重算命令
```bash
docker exec flarum sh -c "cd /flarum/app && php flarum fof:gamification:resyncUsers"
```
这会扫全表, 按 `SUM(v.value) WHERE p.user_id = u.id` 逻辑重算每个用户的 votes。

**备选**: 手写 SQL 直接跑
```sql
UPDATE flarum_users u SET u.votes = COALESCE((
  SELECT SUM(v.value)
  FROM flarum_post_votes v
  INNER JOIN flarum_posts p ON p.id = v.post_id
  WHERE p.user_id = u.id
), 0);
```
关键: `flarum_post_votes.user_id` 是**投票人**, `flarum_posts.user_id` 才是**帖子作者**, 分数加给作者。

### 13.4 全套 CLI 命令 (从 `php flarum list` 里挖到的)
- `fof:gamification:resync` — 重算每个 discussion 的净票数 (用于排行)
- `fof:gamification:resyncUsers` — 重算每个用户的 votes 值
- `fof:gamification:assign-groups` — **按 rank 阈值自动把用户分到对应 Group** (关键, 用于"等级卡板块权限")

### 13.5 教训
- **任何"分数聚合"型插件都可能存在老数据没被 sync 的问题**, 装完先跑一次 recount
- fof/gamification 的 `assign-groups` 命令 + Flarum 原生 Tag Permission 可以**零代码实现"新手不能在拼车板块发帖"** 类需求:
  1. 建 Group (新手/进阶/资深)
  2. `flarum_ranks` 表建阈值 → Group 映射
  3. `assign-groups` 定时跑 (crontab), 用户自动升级
  4. Tag 权限里指定"只有资深 Group 能 Start Discussion"

---

## 14. gamification 投票按钮 `0×0` 尺寸不可见 (最经典的 CSS 埋雷)

### 14.1 现象
Console 探针 `document.querySelector('.Post-vote.Post-upvote')` 返回真实元素, 但 `getBoundingClientRect()` 显示 `width: 0, height: 0`。视觉上帖子右上角只有"回复 赞 编辑 删除"文字按钮, 完全没有 gamification 的按钮痕迹。

### 14.2 诊断链
1. **console 探针一**: 找到 `.Post-vote.Post-upvote` 存在, `display: inline-block` (不是 none), 但尺寸 `0x0` → 说明"存在但被压塌"
2. **探针二 (关键)**: 打印**父级** `<li class="item-votes">` 的 computed style → `display: none` ← **父级被隐了, 子级尺寸自然是 0**
3. **grep 主 CSS**: `grep -oE '.*\.item-votes[^{]*\{[^}]*\}' custom-style-full.css` 直接搜到罪魁: **`.CommentPost .item-votes{display:none!important}`** —— 这是很早以前定制 Post-header 时"觉得投票 UI 是噪音"顺手写的 !important 规则

### 14.3 根因
`display: none` 从父级 `<li>` 渗透 —— 子 `<button>` 无论怎么调自己的样式, 父级 hidden 就整棵子树不占布局空间。这就是为什么 `.Post-vote.Post-upvote` 有 `display: inline-flex` 也没用。

### 14.4 修法
把老规则 flip 掉:
```css
.CommentPost .item-votes{display:inline-flex!important;align-items:center!important;margin:0!important;padding:0!important}
```

### 14.5 教训 (⭐⭐⭐⭐⭐ 必看)
- **"元素存在但看不见"永远先查父链** —— `display: none` 是最容易被忽略的传染源, 因为它不体现在 element 自身的 computed style 里
- **每次装新扩展前先 `grep` 一遍主 CSS 里跟目标 class 相关的老规则**, 避免"我以前隐过的东西现在还想露出来":
  ```bash
  grep -oE "[^,{}]*\.<新class>[^{]*\{[^}]*\}" custom-style-full.css
  ```
- 老代码遗留的 `display: none !important` 是 CSS 定制项目里**最容易踩的雷**, 每次加新功能都可能撞上一个"以前隐掉的"祖先元素

---

## 15. `<style>` + `<svg>` 塞在 custom-header.html 里触发白屏 (最惊悚的一次)

### 15.1 现象
在 `custom-header.html` 里加了一段:
```html
<style>
  table.rankings tr td:first-child{
    background:url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20">...</svg>')...
  }
</style>
```
保存 Custom Header → 前台**整站白屏**, 后台也进不去。

### 15.2 抢救过程
1. **先试 `/admin`** —— 万幸后台还能打开
2. 后台 → Appearance → Edit Custom Header → **全选清空** → Save
3. 硬刷前台 → 恢复正常

如果后台也白屏, 备选是 SQL 直接改:
```bash
docker exec halo-mysql mysql -uflarum -p'XXX' flarum \
  -e "UPDATE flarum_settings SET value='' WHERE \`key\`='custom_header';" 2>/dev/null
docker exec flarum sh -c "cd /flarum/app && php flarum cache:clear"
docker restart flarum
```

### 15.3 根因 (未 100% 确认, 但强烈怀疑)
Flarum 把 `custom_header` 的内容**当 HTML 拼进模板输出**。当模板处理器 (或 Cloudflare 的 HTML 消毒器) 看到 `<style>` 里嵌套 `<svg xmlns=...>`, 可能:
- 把 `<svg>` 当作**真的 HTML 元素**开始解析
- 把 `<style>` 提前闭合
- 导致下面的 HTML 变成孤儿, 整个模板输出被截断
- 浏览器收到不完整的 HTML → 白屏

**同一个 SVG data URI 放在 `custom-style-full.css` (纯 CSS 文件) 里完全正常** —— 差别在于 CSS 文件被浏览器当 stylesheet 直接解析, 不经过 HTML 消毒。

### 15.4 修法
**规避原则** (从此以后严格遵守):
- **custom-header.html 里只放 `<script>` 块**, 不放 `<style>`
- **custom-style-full.css 存所有 CSS**, 包括 SVG data URI
- 需要动态样式时用 JS 修改 `.classList` 或 `element.style`, 不要 `document.head.appendChild(<style>)`

### 15.5 教训
- **配置字段有 sanitizer 就假设有 sanitizer** —— Flarum / Cloudflare / 反代任何一层都可能过滤特定标签组合
- **备用抢救通道要提前想好**:
  1. `/admin` 能打开 → 直接清 Custom Header
  2. 后台也挂 → SSH SQL `UPDATE flarum_settings SET value='' WHERE key='custom_header'`
- **千万不要在 Custom Header 里塞大型 HTML 结构** —— 存 JS 就够了, HTML 该在 template / 扩展里写

---

## 16. CSS `!important` override 是逐属性比拼 (不是逐规则)

### 16.1 现象
`table.rankings .kv-rank-avatar{width:32px!important;height:32px!important;...}` 特异性 (0,2,1), 我以为压过了老规则 `table.rankings td img{display:none!important;max-width:24px!important}` (0,1,2), 结果头像 `display: none` 完全不可见。

### 16.2 根因
CSS 优先级判定**逐属性**做:

| 属性 | 我的规则 (0,2,1) | 老规则 (0,1,2) | 结果 |
|---|---|---|---|
| `width` | `32px !important` | 无 | **我赢** (32px) |
| `height` | `32px !important` | 无 | **我赢** (32px) |
| `border-radius` | `50% !important` | 无 | **我赢** |
| **`display`** | **未设** | **`none !important`** | **老规则赢** (`display: none`) ← 关键 |
| **`max-width`** | **未设** | **`24px !important`** | **老规则赢** (`max-width: 24px`) ← 关键 |

我的规则确实特异性更高, **但只对我实际写的属性生效**。老规则里 `display` 和 `max-width` 因为我没在自己的规则里显式写, 没有更高特异性的竞争者, 就照常应用。

### 16.3 修法
把要覆盖的**每个属性都在自己的规则里显式写出来**:
```css
table.rankings .kv-rank-avatar{
  display:inline-block!important;   /* ← 补上 */
  width:32px!important;
  height:32px!important;
  max-width:32px!important;         /* ← 补上, 抵消老规则的 max-width:24px */
  max-height:32px!important;
  ...
}
```

### 16.4 教训 (⭐⭐⭐⭐⭐)
- **`!important` 是每个 declaration 单独的 flag**, 不是整条规则的
- 想 override 一条 `!important` 规则, 光靠**提高整条规则的特异性**没用, 得**把要覆盖的每个属性都在自己规则里加 `!important`**
- 修 override 时**永远列一个属性对比表**判断哪些属性还没显式覆盖

---

## 17. `::before` 伪元素 `font-size` 不从父级级联

### 17.1 现象
把 `<button>` 的 label 用 CSS `::before content:"贡献加一"` 填充, 想让文字大小跟旁边的"赞"一致 (13px)。给 `<button>` 设 `font-size: 13px !important` 无效, `::before` 生成的文字依然是我早期设的 11px。

### 17.2 根因
`::before` / `::after` 伪元素**不完全从父级继承 CSS**, 特别是 `font-size` 这种可继承属性, 在 Chrome/Firefox 上如果伪元素**自己设了** `font-size`, 就固定用自己的值, 父级怎么改都不生效。

我早期写的规则是:
```css
.Button-label::before{content:"贡献加一"!important;font-size:11px!important}
```
`font-size: 11px` 直接绑在伪元素上, 后来在父按钮上写 `font-size: 13px` 完全无效。

### 17.3 修法
直接改**伪元素自己的规则**:
```css
.Button-label::before{content:"贡献加一"!important;font-size:13px!important}
```

### 17.4 教训
- **`::before` / `::after` 有自己独立的样式系统**, 尤其是它显式设过的属性, 父级怎么改都不会覆盖
- CSS 里给伪元素设 `content` 的地方**顺手一次性把 font 相关都写完**, 别把 `content` 和 `font-size` 分散在不同规则里 (维护地狱)

---

## 18. DOM 探针误报: console.table 输出截断 (弹坑之弹坑)

### 18.1 现象
让用户跑诊断脚本, 用 `console.table(out)` 输出对象数组。看到的 `path` 字段是:
```
ARTICLE.CommentPost.Post.Post--by-actor.Post--by-s... > BUTTON.Button.Button--link > SPAN.Button-label
```
我据此判断"button 是 article 的直接子级"(因为 3 段之间没别的元素), 于是加了 `.CommentPost > .Button.Button--link{display:none}` 规则去隐, 完全无效。

### 18.2 诊断
换成 `console.log(...)` 逐行打印父链, 输出:
```
0: SPAN.Button-label
1: BUTTON.Button.Button--link
2: LI.item-reply
3: UL
4: ASIDE.Post-actions
5: DIV
6: ARTICLE.CommentPost.Post.Post--by-actor.Post--by-start-user
```
真实 DOM 结构完全不是"直接子级", 中间有 4 层元素被 `console.table` 截断隐藏了。

### 18.3 根因
`console.table` 的每个单元格有**宽度上限**, 长字符串会被中间用 `...` 截断, 只保留头尾。看似完整的 `a > b > c` 其实中间可能有 `a > x > y > z > b > c` 被吃掉。

### 18.4 修法
诊断脚本一律用 `console.log` 逐行打印, 别用 `console.table`:
```js
// ❌ 错: 会截断
console.table(pathArray);

// ✅ 对: 逐条输出, 不截断
pathArray.forEach(function(p, i){ console.log(i, p); });
```

### 18.5 教训
- **诊断阶段永远优先 `console.log`**, 不用 table / group / dir 这种"美化"输出方式
- 用户复制 Console 输出给我时, 要求他们**从上到下完整贴**, 不要截图 (截图容易漏行)

---

## 19. flarum-likes 和 gamification 视觉混淆 (设计层面问题)

### 19.1 现象
帖子头部灰条一排显示: `回复 [gamification 按钮] 赞 编辑 删除`

- **"赞"** = flarum-likes (文字按钮, 走 `flarum_post_likes` 表)
- **[gamification 按钮]** = fof/gamification 的 `.Post-vote.Post-upvote` (纯 icon-only, `fa-thumbs-up`, 无 label 文本)

用户视觉上完全分不清两个 👍, 每次点错了 → **flarum-likes 触发但 gamification 不触发** → 分数没加。

### 19.2 修法
给 gamification 按钮加一个**明显区分**的视觉:
- 隐藏 icon: `.Post-vote .Button-icon{display:none}`
- 用 `::before content` 填文本 label: `.Post-vote.Post-upvote .Button-label::before{content:"贡献加一"}`
- 加胶囊底/边框区分 (早期方案, 后来纯文字也行)

现在按钮显示为 "**贡献加一** / **贡献减一**", flarum-likes 显示为"**赞** / **取消赞**", 一眼分辨。

### 19.3 教训
- **装多个投票插件必检查 UI 冲突** —— 按钮外观类似 = 用户永远点错
- 空 label 的 icon-only 按钮**极难被发现**, 尤其挤在文字按钮堆里
- 遇到重叠功能应该: (a) 禁用其中一个; (b) 视觉上明显区分; **不能保持默认**

---

## 20. AlertManager 提示条溢出白卡框外

### 20.1 现象
Flarum 的错误 toast (比如"标题不能为空") 出现在**视口左下角**, 完全跑出主内容白卡的边框范围, 看起来像 bug。

### 20.2 根因
Flarum 默认给 `.AlertManager` 设 `position: fixed; bottom: 0; left: 0; right: 0`。而我们的主内容有 `max-width: 1320px; width: 94%; margin: 0 auto` 居中样式, alert 却仍然贴着**视口边**。

### 20.3 修法
把 AlertManager 的 `left` / `right` 也对齐主内容框:
```css
.AlertManager,.AlertManager > ul,.AlertManager > ul > li{
  left:  max(3vw, calc((100vw - 1320px) / 2)) !important;
  right: max(3vw, calc((100vw - 1320px) / 2)) !important;
}
```
`max(3vw, calc((100vw - 1320px) / 2))` 精确匹配主内容框的左右边距: 视口窄时用 3vw (对应 width:94% 的两侧留白), 视口超过 1400 时用 `(100vw - 1320px) / 2` (对应 max-width:1320px 的居中偏移)。

### 20.4 教训
- Flarum 里所有 `position: fixed` 的元素 (Alert / Composer / Modal-backdrop) 都可能溢出主内容框, 定制时都要单独对齐
- CSS `max(a, b)` 是响应式对齐的神器, 一条规则同时处理窄屏和宽屏两种场景

---

## 21. MySQL 命令行密码警告 (小骚扰)

### 21.1 现象
每次 `docker exec halo-mysql mysql -uflarum -p'PASSWORD' flarum -e "..."` 都会打出:
```
mysql: [Warning] Using a password on the command line interface can be insecure.
```
虽然不影响查询, 但输出很脏。

### 21.2 修法 (三选一)

**方案 A: `MYSQL_PWD` 环境变量** (最干净, 也最安全)
```bash
docker exec -e MYSQL_PWD='PASSWORD' halo-mysql mysql -uflarum flarum -e "..."
```
不会触发 warning, 而且密码不会出现在 `ps aux` 里。

**方案 B: `2>/dev/null`** (最快)
```bash
docker exec halo-mysql mysql -uflarum -p'PASSWORD' flarum -e "..." 2>/dev/null
```
把 stderr 扔了。代价: 真的 SQL 报错也看不到, debug 时得暂时去掉。

**方案 C: 定义 shell 函数** (一劳永逸, 推荐日常使用)
在 `~/.bashrc` 加:
```bash
fdb(){ docker exec -e MYSQL_PWD='PASSWORD' halo-mysql mysql -uflarum flarum -e "$*"; }
```
以后所有 SQL 变成:
```bash
fdb "SELECT id, username, votes FROM flarum_users ORDER BY votes DESC LIMIT 10;"
```

### 21.3 教训
- 命令行传密码是 anti-pattern, 用环境变量或 `~/.my.cnf` 更专业
- 定制运维一个项目时**shell 函数别名可以极大降低认知负担**, 早点定义早爽

---

## 22. Custom Styles 65000 字符上限的应对 (二次遭遇)

### 22.1 背景
第一次踩坑记录在 §4.4, 主要靠**压缩空格 + 合并选择器 + CSS 变量替换**解决。这次做后端集成过程中反复接近上限:
- 66000 → 65000 (被拒)
- 加新功能 (领奖台 / 头像 / 表情面板) 又逼近 63000+

### 22.2 新经验: 把 CSS 分流到 custom-header.html 的 `<style>` 里
一度尝试把新样式塞到 custom-header.html 里的 `<style>` 块以避开 65000 限制。**结果触发了第 15 节的白屏事故**, 教训惨痛。

**最终结论**: **CSS 只能进 `custom-style-full.css`**。custom-header.html 里绝对不放 `<style>`。65000 字符是硬约束, 只能靠压缩 / 删老规则 / 精简选择器。

### 22.3 压缩策略汇总 (跟 §4.4 补充)
1. 删掉已经无用的老规则 (那种 `display:none !important` 类容易堆积)
2. 合并同 body 的选择器为逗号列表
3. 用 `:where()` 降低特异性但 `:where()` 在旧浏览器不支持, 谨慎
4. CSS 变量 (`--kv-xxx`) 替换重复的长值
5. 短属性 (`m:0` 代替 `margin:0` 不合法, 别自作聪明)

### 22.4 教训
- **CSS 空间预算要主动管理**, 不能"等报错才处理"
- 定制大项目做到一半发现 CSS 超限是常见的, 提前 grep 找无用规则清理是最省事的
- **别再尝试把 CSS 塞进 HTML 头文件的 `<style>` 里**, 见第 15 节

---

## 23. Docker 容器不存 config.php 到默认 WORKDIR

### 23.1 现象
`docker exec flarum cat config.php` 报 `No such file or directory`, 但 `docker exec flarum cat /flarum/app/config.php` 有内容。

### 23.2 根因
镜像的默认 WORKDIR 不是 `/flarum/app`, 而是别的地方 (可能是 `/`)。config.php 在 `/flarum/app/config.php` 是绝对路径, 用相对路径读取需要先 `cd`。

### 23.3 修法
一律用绝对路径, 或者用 `-w` 指定 WORKDIR:
```bash
docker exec -w /flarum/app flarum cat config.php
# 或
docker exec flarum cat /flarum/app/config.php
```

### 23.4 教训
- 与镜像交互前先 `docker exec flarum pwd` 确认工作目录
- Flarum 相关的所有文件都在 `/flarum/app/` 下 (config.php / vendor/ / storage/ 等), 记住这个绝对路径省很多事

---

# 24. commit 关键节点 (第二阶段)

按时间顺序, 挑几个决定性的:

| Commit (前 7 位) | 主题 | 重要性 |
|---------|------|-------|
| `2fb0f44` | 修正 Post-actions 左右拉伸导致"回复"跑到左上角 (根因: `left:-14px + right:14px` 拉伸) | ⭐⭐⭐⭐ |
| `168bb0f` | 表情包 100px → 1.5em 文字高度 (纯 inline 显示) | ⭐⭐ |
| `7de29ce` | 自定义表情面板 3 tab (泡泡/阿鲁/颜文字), 干掉原生 regional_indicator 选择器 | ⭐⭐⭐ |
| `114283e` | 帖子行改布局: title + 元信息行 + 右侧 tag, 发帖人可点击 | ⭐⭐⭐⭐ |
| `2136d1d` → `a7d839e` | UserPage 侧栏加卡片 (加入天数/等级/贡献值), 后来发现"鸡腿"是 NodeSeek 品牌, 改成"贡献值" | ⭐⭐ |
| `15a4fbc` | computeXP 优先读真实 fof/gamification 分数 (heuristic 兜底) | ⭐⭐⭐ |
| `906674d` | 字段名根因: fof/gamification 是 `votes` 不是 `points` | ⭐⭐⭐⭐ |
| `5114f55` | 贡献榜按钮 0×0 根因: 父级 `.item-votes{display:none}` 老规则遗留 | ⭐⭐⭐⭐⭐ |
| `f190128` | `::before` 伪元素 font-size 不级联 | ⭐⭐⭐ |
| `606a45a` | 头像被 `table.rankings td img{display:none}` 老规则隐 —— override 得逐属性写 | ⭐⭐⭐⭐⭐ |
| `098cadc` | 把 CSS 从 custom-header.html `<style>` 移回 custom-style-full.css (根因: `<style>` + `<svg>` 触发 Flarum 白屏) | ⭐⭐⭐⭐⭐ |

---

# 25. 附录 (第二阶段追加)

## 25.1 快速诊断链条 (针对"元素在 DOM 但看不见")

1. `document.querySelector('.xxx')` — 有元素返回吗
2. `getBoundingClientRect()` — 尺寸是不是 0×0
3. **父链循环打印** —— 找到第一个 `display: none` 的祖先 (关键)
4. `getComputedStyle(el)` — 看真实的 background / border / display
5. `grep -oE '[^,{}]*\.<class>[^{]*\{[^}]*\}' custom-style-full.css` —— 找哪条老规则在打架
6. 特异性算完 → 补齐要 override 的每个属性

## 25.2 后端方案选择原则 (2GB VPS)

排列组合过后, 最省服务器的做法:
- **不装签到扩展** (增加 DB 每天写入, 而且是"给自己加分"逻辑违背社区激励原则)
- **不写自定义 PHP 扩展** (维护地狱)
- **社区互相点赞就是投喂, 点踩就是扣分** —— 现成的 fof/gamification 已经覆盖 90% 需求, 零新增负载
- **等级卡权限用 Flarum 原生 Group + Tag Permission + gamification 的 assign-groups**, 无扩展依赖

## 25.3 快速抢救预案 (前端定制炸了)

按现场严重程度选一条:

1. **前台白屏但 `/admin` 能进** → Custom Header / Custom Styles 手动清空 Save
2. **`/admin` 也白屏** → SSH SQL 直改
   ```bash
   docker exec halo-mysql mysql -uflarum -p'XXX' flarum \
     -e "UPDATE flarum_settings SET value='' WHERE \`key\`='custom_header';"
   docker exec halo-mysql mysql -uflarum -p'XXX' flarum \
     -e "UPDATE flarum_settings SET value='' WHERE \`key\`='custom_styles';"
   docker exec flarum sh -c "cd /flarum/app && php flarum cache:clear"
   docker restart flarum
   ```
3. **JS 陷入死循环 → 页面转圈不停** → 也走上面 SQL 清空
4. **数据一条都不会丢** —— users / discussions / posts 表全在, 只影响自定义样式和脚本

## 25.4 CDN 引用第三方仓库 (表情/头像等)

本次使用了 3 个公开 CDN 源, 通过 jsdelivr 分发:
- **泡泡表情**: `https://cdn.jsdelivr.net/gh/KBdog/emojiCDN@master/tieba/*.png` (贴吧原生表情 61 张)
- **阿鲁表情**: `https://cdn.jsdelivr.net/gh/liopoos/aru-sticker@master/image/aru (N).png` (281 张)
- **卡通头像**: `https://api.dicebear.com/9.x/avataaars/svg?seed=<name>` (DiceBear API)

**风险规避**:
- 不重打包图片资产, 只引用 URL, 不涉及重分发版权
- 如果原仓库删了/挂了, Fork 一份到自己账号改 URL 即可
- DiceBear 需要注意 API 可用性, localStorage 缓存 base64 data URL 避免每次都请求

---
